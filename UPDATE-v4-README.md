# Cosmic Ascent v4 — update your live deploy

Your game is already hosted at:
  https://erickm76-pixel.github.io/cosmic-ascent/
The build uses FIXED filenames (assets/main.js, assets/main.css) and index.html is
unchanged — so updating to v4 only replaces two files. The URL and your install QR stay valid.

## Fastest update (GitHub web UI)
1. Open your repo: github.com/erickm76-pixel/cosmic-ascent → open the **assets** folder.
2. Click **Add file → Upload files**.
3. From `rabbit-upload/cosmic-ascent-v4/assets/`, drag in **main.js** and **main.css**
   (GitHub replaces the existing ones).
4. **Commit changes.** Wait ~1 minute for Pages to rebuild.
5. On the R1, relaunch the creation (or re-scan the same QR). You're on v4.

(Alternatively, upload the whole `cosmic-ascent-v4/` contents to replace everything —
index.html/icon.png/screenshot.jpg are included but unchanged except the new screenshot.)

## What's new in v4
- Holographic command-console UI: glass panels, telemetry HUD, scanlines, radar sweep,
  data-conduit path, reactor core with integrity arc, targeting lock-lines & contact brackets.
- Controls: scroll-wheel cursor (side button acts), radial quick-build menu, quick actions,
  refined tap targets.
- Depth: tower synergies & adjacency links, tower evolution (Focus/Storm) & fusion,
  commander abilities (Orbital/Overclock/Stasis/Core-Shield on a charge meter),
  and elemental status combos (chill+shock = SHATTER, etc.).
